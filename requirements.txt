# Core
# Pinned exactly — see booking_engine/requirements.txt for why. This dev file
# and the shipped one must agree, or "green locally" stays a different question
# from "green in CI".
fastapi==0.141.1
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP client
httpx>=0.27.0
# Error tracking → GlitchTip (booking_engine/observability.py)
sentry-sdk[fastapi]>=2.70,<3
# WebSocket client (server-side Realtime call control); already present via uvicorn[standard]
websockets>=15.0

# PostgreSQL (Neon)
asyncpg>=0.29.0

# Utilities
python-multipart>=0.0.9
phonenumbers>=8.13
twilio>=9.0
mcp>=1.26.0,<2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
respx>=0.22.0
cryptography>=42
