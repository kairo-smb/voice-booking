pydantic-settings>=2.0.0
asyncpg>=0.29.0
# Pinned exactly, unlike its neighbours (2026-09-22). `>=0.115.0` made CI, local
# dev and the deployed image resolve different versions, and 0.141 changed how
# `include_router` builds the route table — an included router stops being
# flattened into `app.routes` and becomes an `_IncludedRouter` with no `.path`.
# That is a test-visible structural change nothing in this repo declared an
# opinion about. 0.141.1 is what CI and kairo-booking-engine-qa were both
# already running, so this pins the status quo rather than moving it.
fastapi==0.141.1
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
httpx>=0.27.0
phonenumbers>=8.13
twilio>=9.0
mcp>=1.26.0,<2.0.0
cryptography>=42
